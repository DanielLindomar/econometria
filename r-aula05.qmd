---
title: "Aula 5 — Formas funcionais"
subtitle: "Curso de R · Unidade 7 — Pacote estatístico R"
author: "Prof. Dr. Lindomar Pegorini Daniel — UNEMAT, Campus de Sinop"
webr:
  packages:
    - dplyr
    - ggplot2
engine: knitr
---

::: {.logo-topo}
![](logo-unemat.png){width="110"}
:::

::: {.callout-note}
## Objetivos da aula

- estimar a equação **minceriana** (log-lin) e ler retornos percentuais;
- modelar **não linearidade** com termos quadráticos;
- estimar o modelo de **crescimento** do PIB (log-lin com tendência).
:::

## A equação minceriana

Com o salário em log, o coeficiente da escolaridade vira **retorno percentual** — a forma padrão da economia do trabalho.

```{webr}
library(dplyr)
salario <- read.csv("dados/salario_hora2.csv")
minc <- lm(log(salhora) ~ esc + exp, data = salario)
summary(minc)
```

::: {.callout-tip}
## Como ler essa saída

O coeficiente de `esc` multiplicado por 100 é o **retorno da educação**: cada ano de estudo eleva o salário em torno desse percentual, mantida a experiência. É a leitura log-lin: variações absolutas em x, percentuais em y.
:::

## Experiência ao quadrado: o ciclo da vida

Salários sobem com a experiência, mas a taxas decrescentes. O termo `I(exp^2)` captura a concavidade.

```{webr}
minc2 <- lm(log(salhora) ~ esc + exp + I(exp^2), data = salario)
summary(minc2)
```

::: {.callout-tip}
## Como ler essa saída

Sinal positivo em `exp` e **negativo** em `I(exp^2)`: o perfil é uma parábola côncava. O pico ocorre em exp* = b₁/(2·|b₂|) anos — calcule com os coeficientes: é a experiência em que o salário esperado atinge o máximo.
:::

### Exercício 1 — o pico da parábola

Extraia os coeficientes e calcule a experiência do pico salarial.

```{webr}
#| exercise: a5_ex1
b <- coef(minc2)
pico <- b["exp"] / (2 * abs(b["______"]))
pico
```

::: {.callout-tip collapse="true" title="Dica"}
O nome do coeficiente quadrático é `I(exp^2)` — use `b["I(exp^2)"]`.
:::

::: {.callout-note collapse="true" title="Solução"}
```r
b <- coef(minc2)
pico <- b["exp"] / (2 * abs(b["I(exp^2)"]))
pico
```
:::

## O modelo de crescimento

```{webr}
pib <- read.csv("dados/pib_real.csv") |> mutate(t = seq_len(n()))
cresc <- lm(log(pib_real) ~ t, data = pib)
summary(cresc)
exp(coef(cresc)["t"]) - 1
```

::: {.callout-tip}
## Como ler essa saída

O coeficiente de `t` (~0,0315) é a taxa de crescimento contínua; `exp(b) − 1` converte para a taxa anual composta (~3,2% a.a.). Uma linha final de código transformou o coeficiente em número de manchete.
:::

### Exercício 2 — semielasticidade exata

No modelo minceriano, converta o retorno da escolaridade para o percentual **exato** com `exp(b) − 1`.

```{webr}
#| exercise: a5_ex2
b_esc <- coef(minc)["______"]
(exp(______) - 1) * 100
```

::: {.callout-tip collapse="true" title="Dica"}
Complete com `"esc"` e `b_esc`. Para coeficientes pequenos, aproximação e valor exato quase coincidem.
:::

::: {.callout-note collapse="true" title="Solução"}
```r
b_esc <- coef(minc)["esc"]
(exp(b_esc) - 1) * 100
```
:::

## Glossário da aula

| Função | O que faz |
|---|---|
| `I(x^2)` | Termo quadrático na fórmula. |
| `coef(m)["nome"]` | Extrai um coeficiente pelo nome. |
| `abs()` | Valor absoluto. |
